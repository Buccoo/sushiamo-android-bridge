# Copy to keystore.properties (never commit real values)
# storeFile can be relative to this file, e.g. ./release.jks
storeFile=./release.jks
storePassword=REPLACE_STORE_PASSWORD
keyAlias=REPLACE_KEY_ALIAS
keyPassword=REPLACE_KEY_PASSWORD
